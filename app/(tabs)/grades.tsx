import { View } from "react-native";
import { Text } from "react-native-gesture-handler";

export default function GradesScreen() {
    return (
        <View>
            <Text style={{ color: "#222" }}>HEllo Grades</Text>
        </View>
    );
}
